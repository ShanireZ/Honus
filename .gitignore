# === .NET (agent/) ===
[Bb]in/
[Oo]bj/
.vs/
*.user
*.suo
*.nupkg
*.binlog
[Tt]est[Rr]esults/

# === 编辑器 ===
.vscode/
.idea/
*.swp

# === Horus 运行时 / 机密(切勿提交) ===
# 真实配置含 PSK / 管理令牌 / 视觉 API key —— 只提交 *.sample.json,真值放 *.config.json
agent.config.json
**/agent.config.json
server.config.json
**/server.config.json
# Agent 断网缓冲
**/buffer/
# 服务器运行时:SQLite 库与证据截图
*.db
*.db-wal
*.db-shm
*.sqlite
/images/
/archive/

# === 证书 / 环境机密(切勿提交) ===
.env
.env.*
!.env.example
*.pfx
*.snk
*.key
*.pem
*.log

# === 依赖(未来服务器端) ===
node_modules/

# === OS ===
Thumbs.db
.DS_Store
desktop.ini

# 注意:agents.md 为项目准则,必须跟踪,勿在此排除。
