# 2026-07-21 工作日志

## 全量代码审查（Horus）
- 范围：contracts / agentcore / agent / server / tests 全部源码 + architecture/AGENTS.md。
- 产出：`docs/code-review-2026-07-21.md`（197KB，110 行）。
- 发现汇总：
  - BUG/逻辑错误 6 项：#1 ImageIngest body 无独立大小上限（中，Kestrel 2MB 兜底）、#2 ParseConfidence 对 1.0 边界歧义（低）、#3 BrowserUrlSource 每 2s 全树 UIA 遍历读地址栏（中·脆弱低效）、#4 ScreenshotCapturer 只抓主显示器（中·多屏盲区）、#5 空 seatId 可插入 exams（低）、#6 Envelope 外层 seq 与 @event.seq 冗余（低）。
  - 功能待完善 7 项（M5 真机验收 / M4 both→oidc 灰度 / ClipboardWatcher / ForegroundWindow / ProcessWatcher ETW / 多显示器 / 击键埋点移除）。
  - 代码质量 6 项、性能 5 项。
  - 已确认正确抽样：canonical 逐字节一致、seq 高水位持久化、读写分离、OIDC RS256 离线验签、归档幂等、管理面 HttpOnly+SameSite 鉴权。
- 优先级：P1 = BUG#1 / BUG#3；P2 = BUG#4 / BUG#2 / 质量#2/#5；P3 = 其余。
- 结论：多轮独立审计后工程质量高，核心安全不变量扎实，剩余为改进项。

## P1+P2 修复（同日下午）
按优先级从高到低逐个修复并验证，整体 `dotnet test` 271 全绿（0 失败）。
- **BUG#1**（ImageIngest 缺 body 上限）：新增 `server/Ingest/IngestBody.cs` 共享 helper（`ReadWithLimitAsync`，Content-Length 超限在分配 MemoryStream 前即拒 + 实测兜底），ImageIngest 改用 `MaxImageBodyBytes=2MB` 独立上限（不依赖 Kestrel 全局）。影响：防御未鉴权者缓冲放大。
- **质量#5**（ingest 安全逻辑分叉）：KeystrokeIngest 也改用同一 `IngestBody` helper，两路 body 读取+上限+鉴权顺序统一。新增 `IngestBodyTests`（5 项）锁定行为。
- **BUG#3**（BrowserUrlSource 脆弱全树遍历）：重写地址栏定位——同 hwnd 缓存 AutomationElement 跳过全树遍历；失效才按「toolbar→edit」窄条件重探，退化全树兜底；抽出 `LooksLikeUrl/ReadValue`。缓存命中时几乎零 UIA 开销。
- **BUG#4**（多显示器盲区）：`ScreenshotCapturer` 改 `Screen.AllScreens` 遍历各屏各抓一张；每屏按设备名独立 pHash 去重；主屏(Primary) imageId 作事件关联锚点；trigger 多屏时追加 `:screen{i}`（不影响 isEvent/基线抽样/backstop 过滤）。代价：基线/触发存储与上传约 ×屏数。
- **BUG#2**（ParseConfidence 1.0 边界漏报）：用 `JsonElement.GetRawText()` 是否含小数点区分「整数 1(=1%·保持 F9 不放大)」与「1.0(=100% 归一化概率·放大防漏报)」；原 `1.0→1` 致 confidence:1 且 suspicious:true 因 1<阈值漏报已修复。新增测试 `小数1dot0放大到100` 锁定；原 `整数1保持1_不放大` 仍成立。
- **质量#2**（风险分魔法数）：新增 `contracts/RiskScores.cs` 集中常量，RiskModel.Derive / KeystrokeIngest.RiskFrom / BrowserUrlSource / agent/Program.cs(M5) 全部引用，Agent 自报与服务器复判共用一份，避免漂移。
- 注：BUG#3/BUG#4 属 Windows GDI/UIA/多屏运行时行为，仅编译验证（全方案 0 警告 0 错误），完整运行时需真机 smoke（浏览器+多显示器），与架构"诚实标注盲区/待 owner 真机验收"一致。
- P3（BUG#5 空 seatId / BUG#6 Envelope 冗余 / 性能与可读性等）留待下一轮。

## P3 推进（当晚 · 全部完成 · 279 测试全绿 0 回归 · 全量编译 0 警告）
按"本轮推进所有其他未修复/完善/质量/性能项"推进，13 项编码+测试改动落地，整体 `dotnet test` 279 通过 0 失败。

### BUG 类
- **BUG#5 空 seatId 可插入**：`server/Api/Endpoints.cs` 的 `POST /api/exams` 座位校验新增 `sid.Length==0 → BadRequest(empty_seatId)`（空 seatId 不进 seats 表，防脏数据污染席位关联/看板）。`bad_seatId`(IsSafeId) 校验保持。
- **BUG#6 Envelope seq 冗余**：`server/Ingest/EventIngest.cs` 改以内层 `e.seq`(被 HMAC 签名)为权威；外层 `frame.seq` 仅作一致性告警（不一致仅 `LogWarning`，不以外层覆盖）。修复初版引用未声明变量的编译错（CS0841），告警只带 outerS/seq。

### 质量类
- **质量#6 async void 异常兜底**：`agent/Program.cs` Main 开头注册 `TaskScheduler.UnobservedTaskException += (_,ea)=>{ writes stderr; ea.SetObserved(); }`，防 fire-and-forget 任务异常致进程静默崩溃。

### 第二节功能补全（采集端硬化 / 身份灰度）
- **ClipboardWatcher 干净关停**：`agent/Signals/ClipboardWatcher.cs` Stop 改 `PostMessage(_win.Handle, WM_USER+1)` 向 STA 窗口投自定义消息，`WndProc` 处理 `WmExit` 调 `Application.ExitThread()`（必须本线程）；去掉 `NativeWindow.BeginInvoke`（CS1061，NativeWindow 无该方法）。
- **ForegroundWindowSource 事件驱动化**：`agent/Signals/ForegroundWindowSource.cs` 重写——`SetWinEventHook(EVENT_SYSTEM_FOREGROUND, WINEVENT_OUTOFCONTEXT)` 前台窗口切换才上报，STA 消息泵驱动；Stop 用 `PostThreadMessage(WM_QUIT)` 退出；Dispose `Join` 线程；持有 `_hookDelegate` 防 GC 回收回调。
- **ProcessWatcher 事件驱动确认**：`agent/Signals/ProcessWatcher.cs` 已是 `ManagementEventWatcher`(__InstanceCreation/DeletionEvent) 事件驱动非轮询；更新过时 TODO 注释说明事件驱动已满足低延迟意图，ETW 为可选未来方案（非本轮必做）。
- **M5 真机验收自动化**：`tests/FixesTests.cs` 新增 `M5SignalServerRiskTests`（5 项：ScreenshotObscured=60 / CapabilityDegraded=55 / WatchdogRestart=55 / SuspectedSuspend=0 / 四信号均归 source='health'），把 M5 风险分与"health 单列"行为用单测锁定（替代 owner 真机手测的回归基线）。
- **both→oidc 灰度落地**：`tests/FixesTests.cs` 新增 `AuthModeGrayMigrationTests`（3 项：psk 接受 PSK 非 OIDC / oidc 拒 PSK 开 OIDC / both 共存），锁定 M4 AuthMode 三者语义。

### 性能类
- **性能#1 视觉分析并发**：`server/Config/ServerConfig.cs` 新增 `VisionConcurrency`(默认1·钳1~8)；`server/Program.cs` 读 `HORUS_VISION_CONCURRENCY`；`server/Analysis/Vision/VisionAnalysisService.cs` 执行循环改"每轮批量拉 N=concurrency 张并行 `Task.WhenAll`"，保留 `SingleReader=true`、默认 concurrency=1 行为不变、统一异常兜底+`_inflight` 释放。
- **性能#2 /seats 聚合**：`server/Api/Endpoints.cs` 的 `/seats` 由"每座位多个相关子查询"改为 5 个 CTE(hb/ev/hlt/sq/sess)各自 GROUP BY 后单次 LEFT JOIN。列序号保持调用点一致(5=last_hb…7=max_risk…8=ev_count…9=susp…19=health)。**首次回归**：① 嵌套 `MAX(MAX(...))` SQLite 禁聚合嵌套 → 拆为 `MAX(risk)`+`MAX(server_risk)` 外层标量 `MAX(a,b)` 取大；② `ev.ev_count` 漏 `COALESCE` → LEFT JOIN 无事件座位 `GetInt32(8)` 抛 "data is NULL at ordinal 8" 致 500。两处修复后 279 全绿。
- **性能#3 IntegrityAudit 流式**：`server/Analysis/IntegrityAudit.cs` `Run` 由"一次性全考试拉入 Dictionary"改"按 agent_id 边界流式调用 `ProcessAgent` 闭包"，峰值内存降为单 agent 缓冲。
- **性能#4 索引入库**：`server/Data/Schema.cs` `Migrate` 末尾新增 4 个幂等索引(AddIndexIfMissing)：ix_images_exam_id / ix_suspicious_queue_exam_src_status / ix_events_exam_type / ix_events_exam_ts。新增 `AddIndexIfMissing`(查 sqlite_master 同名避免重复 CREATE)。
- **性能#5 Db 只读池可配置**：`server/Config/ServerConfig.cs` 新增 `ReadPoolSize`(默认4)；`server/Program.cs` 读 `HORUS_READ_POOL` 并 `new Db(dataSource, cfg.ReadPoolSize)`。

### 验证
- `dotnet test Horus.sln -c Debug`：**通过 279 / 失败 0 / 跳过 0**（较 P1+P2 的 271 增 8 项新测试：M5 5 + both→oidc 3）。
- `dotnet build Horus.sln -c Debug`：已成功生成，0 警告 0 错误。
- 注：BUG#3/BUG#4（浏览器地址栏/多屏）等 Windows 运行时行为仍仅编译验证，完整运行时待 owner 真机 smoke（与架构"诚实标注盲区"一致）。
- 临时复现脚本 `repro_seats.py` 已删（仅用于定位 /seats CTE 的 500 根因）。
